const questions=[
{step:'経営目的',q:'今、経営者として最も実現したいことは何ですか？',hint:'AIありきではなく、経営目標を選んでください。',key:'goal',type:'single',options:[['sales','売上・新規顧客を増やす'],['margin','粗利・利益率を改善する'],['productivity','人手不足を補い、生産性を上げる'],['quality','判断・サービス品質を安定させる'],['speed','意思決定や対応速度を上げる']]},
{step:'経営目的',q:'AIに最も期待している役割は何ですか？',hint:'現時点の期待で構いません。',key:'expectation',type:'single',options:[['automate','定型業務を自動化する'],['decision','経営・現場の判断を支援する'],['knowledge','社内情報を探し、知識を再利用する'],['forecast','売上・需要・リスクを予測する'],['create','提案・企画・文章・資料を作る']]},
{step:'経営目的',q:'AI導入について経営者自身の優先度はどの程度ですか？',hint:'1＝低い、5＝最優先',key:'commitment',type:'scale'},
{step:'意思決定',q:'日常的に重要な判断を行う業務はどこですか？',hint:'最も経営インパクトが大きいものを選択。',key:'decisionArea',type:'single',options:[['sales','営業・案件選別・顧客提案'],['estimate','見積・原価・価格決定'],['operations','生産・施工・配車・シフト等の運用'],['customer','顧客対応・問い合わせ・クレーム'],['backoffice','採用・経理・総務・契約等の管理業務']]},
{step:'意思決定',q:'その判断は、どの程度「経験や勘」に依存していますか？',hint:'5ほど属人性が高い状態です。',key:'intuition',type:'scale'},
{step:'意思決定',q:'その判断を行うために、複数の資料や情報を集めていますか？',hint:'メール、Excel、PDF、顧客情報、過去案件など。',key:'infoFragmentation',type:'scale'},
{step:'意思決定',q:'判断に必要な情報収集・整理に時間がかかっていますか？',hint:'5ほど負荷が大きい状態です。',key:'decisionLoad',type:'scale'},
{step:'意思決定',q:'過去の判断結果を振り返り、成功・失敗理由を再利用できていますか？',hint:'5＝十分できている。',key:'learning',type:'scale'},
{step:'業務プロセス',q:'反復的な入力・転記・照合・集計・資料作成は多いですか？',hint:'AIや自動化の基本的な効果余地を確認します。',key:'repetition',type:'scale'},
{step:'業務プロセス',q:'文章、画像、音声、PDFなど非構造データを扱う業務は多いですか？',hint:'生成AIが得意とする領域です。',key:'unstructured',type:'scale'},
{step:'業務プロセス',q:'同じ質問への回答、提案、説明を繰り返す業務は多いですか？',hint:'営業・CS・社内問い合わせ等を含みます。',key:'repeatKnowledge',type:'scale'},
{step:'業務プロセス',q:'その業務で、ミス・漏れ・品質ばらつきが経営課題になっていますか？',hint:'5ほど品質改善余地が大きい状態です。',key:'qualityGap',type:'scale'},
{step:'データ',q:'過去の案件・顧客・売上・原価・対応履歴等がデジタルで蓄積されていますか？',hint:'5＝十分蓄積されている。',key:'dataVolume',type:'scale'},
{step:'データ',q:'必要なデータに担当者がアクセスしやすい状態ですか？',hint:'5＝必要なデータをすぐ取得できる。',key:'dataAccess',type:'scale'},
{step:'データ',q:'データの形式や項目はある程度統一されていますか？',hint:'5＝表記・項目が整理されている。',key:'dataQuality',type:'scale'},
{step:'導入条件',q:'AI導入を担当・推進する責任者を決められますか？',hint:'5＝すぐ決められる。',key:'owner',type:'scale'},
{step:'導入条件',q:'まず1業務で4〜12週間のPoCを行うことは可能ですか？',hint:'5＝すぐ検証可能。',key:'poc',type:'scale'},
{step:'導入条件',q:'AIに触れさせにくい機密情報・個人情報・規制要件はどの程度ありますか？',hint:'5＝制約が非常に大きい。',key:'risk',type:'scale'}];

const state={answers:{},index:0};
const root=document.getElementById('questionRoot');
const nextBtn=document.getElementById('nextBtn');
const prevBtn=document.getElementById('prevBtn');
document.querySelectorAll('[data-scroll]').forEach(b=>b.addEventListener('click',()=>document.querySelector(b.dataset.scroll).scrollIntoView({behavior:'smooth'})));
function render(){const item=questions[state.index];document.getElementById('stepLabel').textContent=`STEP ${['経営目的','意思決定','業務プロセス','データ','導入条件'].indexOf(item.step)+1} / 5 · ${item.step}`;document.getElementById('questionLabel').textContent=`質問 ${state.index+1} / ${questions.length}`;const current=state.answers[item.key];let body='';if(item.type==='single')body=`<div class="options">${item.options.map(([v,l])=>`<label class="option ${current===v?'selected':''}"><input type="radio" name="current" value="${v}" ${current===v?'checked':''}><span>${l}</span></label>`).join('')}</div>`;else body=`<div class="options">${[1,2,3,4,5].map(v=>`<label class="option ${current===v?'selected':''}"><input type="radio" name="current" value="${v}" ${current===v?'checked':''}><span><b>${v}</b>　${['ほとんどない','やや少ない','中程度','かなりある','非常に高い'][v-1]}</span></label>`).join('')}</div>`;root.innerHTML=`<div class="question-card"><div class="eyebrow">${item.step}</div><h3>${item.q}</h3><p class="hint">${item.hint}</p>${body}</div>`;root.querySelectorAll('input').forEach(i=>i.addEventListener('change',e=>{state.answers[item.key]=item.type==='scale'?Number(e.target.value):e.target.value;render();}));prevBtn.disabled=state.index===0;nextBtn.textContent=state.index===questions.length-1?'診断カルテを作成 →':'次へ →';updateProgress();}
function updateProgress(){const done=Object.keys(state.answers).length,pct=Math.round(done/questions.length*100);document.getElementById('progressText').textContent=pct+'%';document.getElementById('progressBar').style.width=pct+'%';document.getElementById('progressHint').textContent=done===questions.length?'回答完了。診断カルテを生成できます。':`${done}/${questions.length}問回答済み`;}
function requireAnswer(){const item=questions[state.index];if(state.answers[item.key]===undefined){alert('この質問に回答してください。');return false}return true}
nextBtn.addEventListener('click',()=>{if(!requireAnswer())return;if(state.index<questions.length-1){state.index++;render()}else diagnose()});prevBtn.addEventListener('click',()=>{if(state.index>0){state.index--;render()}});
const areaNames={sales:'営業・案件判断',estimate:'見積・原価・価格決定',operations:'生産・施工・運用最適化',customer:'顧客対応・問い合わせ',backoffice:'管理・バックオフィス'};
function avg(keys){return keys.reduce((s,k)=>s+(state.answers[k]||0),0)/keys.length}function clamp(n,min=0,max=100){return Math.max(min,Math.min(max,n))}function bar(name,value){return `<div class="chart-row"><span>${name}</span><i style="--w:${value}%"></i><b>${value}</b></div>`}
function buildPriorities(a,fit,data,feas){const base=[{name:areaNames[a.decisionArea],score:Math.round((a.decisionLoad+a.intuition+a.infoFragmentation)*6+fit*.25),reason:'経営判断に直結し、情報収集・属人判断の改善余地が大きい領域です。',solution:'意思決定支援AI / RAG / 案件スコアリング'},{name:'定型・反復業務の自動化',score:Math.round((a.repetition+a.repeatKnowledge)*8+feas*.2),reason:'入力・転記・集計・定型回答など、短期間で効果検証しやすい領域です。',solution:'生成AI＋ワークフロー自動化'},{name:'社内ナレッジ活用',score:Math.round((a.unstructured+a.infoFragmentation+(6-a.learning))*6+data*.2),reason:'PDF・メール・過去案件を検索・再利用し、担当者依存を減らせます。',solution:'社内RAG / AI検索 / FAQエージェント'},{name:'予測・経営分析',score:Math.round((a.dataVolume+a.dataQuality+a.decisionLoad)*6+data*.25),reason:'蓄積データが一定量あれば、需要・売上・原価・案件確度の予測候補になります。',solution:'予測モデル / BI＋AI分析'},{name:'顧客・営業対応支援',score:Math.round((a.repeatKnowledge+a.unstructured+a.qualityGap)*6+fit*.2),reason:'提案・問い合わせ・議事録・フォローを標準化し、対応速度と品質を上げられます。',solution:'営業Copilot / 顧客対応AI'}];return base.map(x=>({...x,score:clamp(x.score)})).sort((x,y)=>y.score-x.score).slice(0,3)}
function summaryText(score,a,data,fit){if(score>=75)return `AI導入効果が出やすい状態です。特に「${areaNames[a.decisionArea]}」を起点に、全社導入ではなく1業務PoCから始めるのが合理的です。`;if(score>=58)return `AI適合性は十分ありますが、データ整備や業務定義を同時に進める必要があります。まず効果測定しやすい1業務に限定してください。`;if(score>=42)return `AIそのものより、業務整理・データ整備・責任者設定が先です。条件を整えた上で小規模PoCを推奨します。`;return `現時点ではAI導入を急ぐより、業務標準化とデータ蓄積を優先した方が投資対効果を高められます。`}
function diagnose(){const a=state.answers;const impact=clamp(Math.round(avg(['decisionLoad','repetition','qualityGap','intuition'])*14+a.commitment*6));const fit=clamp(Math.round(avg(['repetition','unstructured','repeatKnowledge','infoFragmentation','decisionLoad'])*20));const data=clamp(Math.round(avg(['dataVolume','dataAccess','dataQuality'])*20));const feasibility=clamp(Math.round(((a.owner+a.poc+(6-a.risk))/3)*20));const governance=clamp(Math.round(((a.commitment+a.owner+a.poc+(6-a.risk))/4)*20));const score=clamp(Math.round(impact*.28+fit*.28+data*.18+feasibility*.16+governance*.10));const label=score>=75?'積極導入を推奨':score>=58?'限定業務からPoC推奨':score>=42?'条件整備後に検証':'現時点ではAI優先度低め';const priorities=buildPriorities(a,fit,data,feasibility);const employees=Number(document.getElementById('employees').value||30);const monthlyHours=Math.round((a.repetition+a.decisionLoad+a.repeatKnowledge+a.infoFragmentation)/20*employees*9);const saveLow=Math.round(monthlyHours*.18),saveHigh=Math.round(monthlyHours*.36),hourly=3500,yenLow=Math.round(saveLow*hourly/10000),yenHigh=Math.round(saveHigh*hourly/10000),speed=Math.max(5,Math.round((a.decisionLoad+a.infoFragmentation+a.intuition)*5));const company=document.getElementById('companyName').value||'貴社';document.getElementById('resultRoot').innerHTML=`<div class="result-grid"><article class="result-card wide result-hero"><div class="result-score"><div><strong>${score}</strong><small>/100</small></div></div><div><span class="badge">${label}</span><h3>${company}のAI導入適性</h3><p>${summaryText(score,a,data,fit)}</p><div class="chart-bars">${bar('経営インパクト',impact)}${bar('AI適合性',fit)}${bar('データ準備度',data)}${bar('実装容易性',feasibility)}${bar('推進条件',governance)}</div></div></article><article class="result-card"><h3>AI化優先領域 TOP3</h3><div class="priority-list">${priorities.map((p,i)=>`<div class="priority-item"><div class="priority-head"><h4>${i+1}. ${p.name}</h4><span class="priority-score">${p.score}</span></div><p>${p.reason}</p><small>推奨AI：${p.solution}</small></div>`).join('')}</div></article><article class="result-card"><h3>期待効果（初期仮説）</h3><div class="metrics"><div class="metric"><strong>${saveLow}〜${saveHigh}h</strong><span>月間削減時間</span></div><div class="metric"><strong>${yenLow}〜${yenHigh}万円</strong><span>月間時間価値</span></div><div class="metric"><strong>${speed}%前後</strong><span>判断速度改善余地</span></div><div class="metric"><strong>${data}</strong><span>データ準備度</span></div></div><p class="disclaimer">削減時間・金額は回答値と従業員規模から算出した簡易推定です。正式提案では対象業務の実測工数、件数、単価、エラー率を確認して再計算します。</p></article><article class="result-card wide"><h3>90日AI導入ロードマップ</h3><div class="roadmap"><div><b>DAY 1–14</b><strong>業務・データ棚卸し</strong><p>最優先領域の判断基準、入力資料、処理、KPIを定義。</p></div><div><b>DAY 15–30</b><strong>PoC設計</strong><p>対象データを限定し、AI出力と人の判断の役割分担を設計。</p></div><div><b>DAY 31–60</b><strong>試作・比較検証</strong><p>時間、品質、再現率、売上寄与等を現行業務と比較。</p></div><div><b>DAY 61–90</b><strong>本導入判断</strong><p>ROI・リスク・現場定着を評価し、拡張または中止を判断。</p></div></div></article><article class="result-card wide"><h3>次回AI提案で確認すべき材料</h3><p>①対象業務の月間件数・工数　②使用資料・データのサンプル　③現行の判断基準　④成功/失敗事例　⑤人件費・外注費　⑥AI利用不可情報　⑦PoC責任者。この7点を取得すると、診断から具体的な要件定義・概算ROI・開発見積へ移行できます。</p></article></div>`;document.getElementById('result').classList.remove('hidden');document.getElementById('result').scrollIntoView({behavior:'smooth'});localStorage.setItem('smeAiDiagnostic',JSON.stringify({company,answers:a,score,priorities,date:new Date().toISOString()}))}
render();
